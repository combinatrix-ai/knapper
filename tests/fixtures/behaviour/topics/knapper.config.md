---
vault_path: .
exclude:
  - Archives/
---

# Topics

A vault that uses `[[COO採用]]` the way a lot of real vaults do: as a label
rather than as a promise that a note by that name exists. `Archives/` is
excluded, so nothing whole-vault may read or rewrite it.
