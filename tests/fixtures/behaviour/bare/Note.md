# Note

A folder of markdown that knapper has never been run against. There is no
`knapper.config.md` here, and that is the point: this is what a vault looks
like the moment before `knapper init`.

It links to [[Nowhere]] so the directory is not inert.
