# Conventions

A vault that documents its own conventions holds checkboxes that are not
tasks, next to ones that are.

- [ ] a real task
- [ ] keep the `- [ ] marker` helper honest
- [ ] a real task %%with a note to self%%

The shape a task takes:

```markdown
- [ ] example inside a code fence
```

%%
- [ ] a task inside a comment
%%
