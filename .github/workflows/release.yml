name: knapper-release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always
  MACOSX_DEPLOYMENT_TARGET: "11.0"

jobs:
  build-knapper:
    name: Build knapper (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            builder: cargo
          - os: macos-latest
            target: x86_64-apple-darwin
            builder: cargo
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            builder: cargo-zigbuild
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            builder: cargo-zigbuild

    steps:
      - name: Check out knapper
        uses: actions/checkout@v4

      - name: Install Rust toolchain for knapper
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          targets: ${{ matrix.target }}

      - name: Validate knapper release version
        shell: bash
        run: |
          set -euo pipefail
          tag_version="${GITHUB_REF_NAME#v}"
          package_version="$(cargo pkgid --package knapper | sed -E 's/.*#([^@]+@)?//')"
          test "$tag_version" = "$package_version" || {
            echo "knapper tag $GITHUB_REF_NAME does not match Cargo package version $package_version" >&2
            exit 1
          }

      - name: Install Zig for knapper Linux cross-builds
        if: matrix.builder == 'cargo-zigbuild'
        shell: bash
        run: |
          set -euo pipefail
          zig_arch="$(case "$(uname -m)" in
            x86_64|amd64) echo x86_64 ;;
            aarch64|arm64) echo aarch64 ;;
            *) echo "unsupported runner architecture: $(uname -m)" >&2; exit 1 ;;
          esac)"
          zig_os="$(case "$(uname -s)" in
            Linux) echo linux ;;
            Darwin) echo macos ;;
            *) echo "unsupported runner OS: $(uname -s)" >&2; exit 1 ;;
          esac)"
          curl --fail --silent --show-error --location --proto '=https' --tlsv1.2 \
            "https://ziglang.org/download/0.15.2/zig-${zig_arch}-${zig_os}-0.15.2.tar.xz" \
            | tar -xJ -C "${RUNNER_TEMP}"
          echo "${RUNNER_TEMP}/zig-${zig_arch}-${zig_os}-0.15.2" >> "${GITHUB_PATH}"

      - name: Install cargo-zigbuild for knapper Linux cross-builds
        if: matrix.builder == 'cargo-zigbuild'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-zigbuild

      - name: Build and package knapper
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ matrix.builder }}" = "cargo-zigbuild" ]; then
            cargo zigbuild --release --locked --target "${{ matrix.target }}" --bins
          else
            cargo build --release --locked --target "${{ matrix.target }}" --bins
          fi

          archive="knapper-${GITHUB_REF_NAME}-${{ matrix.target }}.tar.gz"
          staging="${RUNNER_TEMP}/knapper-package-${{ matrix.target }}"
          rm -rf "$staging"
          mkdir -p "$staging/knapper"
          cp "target/${{ matrix.target }}/release/knapper" "$staging/knapper/knapper"
          cp README.md LICENSE "$staging/knapper/"
          chmod 755 "$staging/knapper/knapper"
          tar -C "$staging" -czf "$archive" knapper

          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$archive" > "${archive}.sha256"
          else
            shasum -a 256 "$archive" > "${archive}.sha256"
          fi

      - name: Attest knapper build provenance
        id: attest
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: knapper-${{ github.ref_name }}-${{ matrix.target }}.tar.gz

      - name: Stage knapper attestation bundle
        shell: bash
        run: |
          set -euo pipefail
          cp "${{ steps.attest.outputs.bundle-path }}" \
            "knapper-${{ github.ref_name }}-${{ matrix.target }}.tar.gz.sigstore.json"

      - name: Upload knapper package
        uses: actions/upload-artifact@v4
        with:
          name: knapper-${{ matrix.target }}
          path: |
            knapper-${{ github.ref_name }}-${{ matrix.target }}.tar.gz
            knapper-${{ github.ref_name }}-${{ matrix.target }}.tar.gz.sha256
            knapper-${{ github.ref_name }}-${{ matrix.target }}.tar.gz.sigstore.json
          if-no-files-found: error

  publish-knapper:
    name: Publish knapper GitHub Release
    runs-on: ubuntu-latest
    needs: build-knapper
    permissions:
      contents: write
      id-token: write
      attestations: write

    steps:
      - name: Check out release notes
        uses: actions/checkout@v4

      - name: Download knapper packages
        uses: actions/download-artifact@v4
        with:
          pattern: knapper-*
          merge-multiple: true

      - name: Create knapper checksum manifest
        shell: bash
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          sha256sum knapper-"${tag}"-*.tar.gz > "knapper-${tag}-checksums.txt"
          cp "knapper-${tag}-checksums.txt" SHA256SUMS

      - name: Attest knapper checksum manifest
        id: attest-manifest
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: knapper-${{ github.ref_name }}-checksums.txt

      - name: Stage knapper manifest attestation bundle
        shell: bash
        run: |
          set -euo pipefail
          cp "${{ steps.attest-manifest.outputs.bundle-path }}" \
            "knapper-${{ github.ref_name }}-checksums.txt.sigstore.json"

      - name: Publish knapper GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          body_path: docs/releases/${{ github.ref_name }}.md
          files: |
            knapper-*
            SHA256SUMS
